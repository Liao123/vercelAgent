/** A024 黄金路径占位页：Agent 复刻 demo 站点时写入此文件。 */
export default function DemoReplicatePage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-zinc-100 p-8">
      <p className="text-sm text-zinc-500">
        demo-replicate 占位页 — 由 Agent 根据 design spec 替换。
      </p>
    </main>
  );
}
